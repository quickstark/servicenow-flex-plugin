import * as React from "react";
import { ThemeProps } from "../../theme";
interface QueueContentProps {
    content: (renderProps: RenderProps) => JSX.Element;
}
interface RenderProps {
    open: boolean;
    toggleDrawer: () => void;
}
interface QueueContentState {
    open: boolean;
}
declare class QueueContent extends React.PureComponent<QueueContentProps & ThemeProps, QueueContentState> {
    state: QueueContentState;
    toggleDrawer: () => void;
    render(): JSX.Element;
}
export { QueueContent };
