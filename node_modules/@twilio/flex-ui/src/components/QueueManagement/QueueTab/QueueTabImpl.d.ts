import * as React from "react";
import { RouteComponentProps } from "react-router-dom";
import { Match } from "../../view/QueuesView/QueuesViewTypes";
interface Props {
    onTabChange: (tabValue: number) => void;
    tabValue: number;
}
export declare type QueueTabProps = Props & RouteComponentProps<Match>;
declare class QueueTab extends React.Component<QueueTabProps> {
    componentDidUpdate(prevProps: QueueTabProps): void;
    handleChange: (e: React.MouseEvent, value: number) => void;
    render(): JSX.Element;
}
export { QueueTab };
