import * as React from "react";
import { AppState } from "../../../state";
declare function mapStateToProps(state: AppState, ownProps: OwnProps): {
    requiredSkills: string;
};
declare type OwnProps = {
    queueSid: string;
    open: boolean;
    onClose: () => void;
};
declare type ReduxStateProps = ReturnType<typeof mapStateToProps>;
export declare type WrappedComponentProps = ReduxStateProps & OwnProps;
export declare function withNoEligibleWorkersDialog(Component: React.ComponentType<WrappedComponentProps>): import("react-redux").ComponentClass<Pick<WrappedComponentProps, "open" | "onClose" | "queueSid">> & {
    WrappedComponent: React.ComponentType<WrappedComponentProps>;
};
export {};
