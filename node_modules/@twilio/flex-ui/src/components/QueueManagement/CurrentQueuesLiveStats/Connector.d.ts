import * as React from "react";
import { AppState } from "../../../state";
declare function mapStateToProps(state: AppState, ownProps: OwnProps): {
    queue: import("../../../state/QueuesState").WorkerQueue;
    availableAgents: number;
    unavailableAgents: number;
    offlineAgents: number;
};
declare type OwnProps = {
    sid: string;
};
declare type ReduxStateProps = ReturnType<typeof mapStateToProps>;
export declare type WrappedComponentProps = OwnProps & ReduxStateProps;
export declare const withQueueStats: (Component: React.ComponentType<WrappedComponentProps>) => import("react-redux").ComponentClass<Pick<WrappedComponentProps, "sid"> & OwnProps> & {
    WrappedComponent: React.ComponentType<WrappedComponentProps>;
};
export {};
