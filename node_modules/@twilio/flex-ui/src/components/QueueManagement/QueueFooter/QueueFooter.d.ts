import * as React from "react";
import { AppState } from "../../../state";
declare function mapStateToProps(state: AppState): {
    shouldHide: boolean;
};
declare type ReduxStateProps = ReturnType<typeof mapStateToProps>;
export declare type WrappedComponentProps = ReduxStateProps;
export declare const withQueueFooter: (Component: React.ComponentType<WrappedComponentProps>) => import("react-redux").ComponentClass<Pick<{
    shouldHide: boolean;
}, never>> & {
    WrappedComponent: React.ComponentType<{
        shouldHide: boolean;
    }>;
};
export {};
